const START = 'START'
const SUCCESS = 'SUCCESS'
const FAILURE = 'FAIL'

const GetPlanets = '@GetPlanets_'

export const GetPlanetsTypes = {
  start: GetPlanets + START,
  success: GetPlanets + SUCCESS,
  failure: GetPlanets + FAILURE,
}
